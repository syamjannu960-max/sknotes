
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // For this sample app, allow anyone to read and write to the database.
    // In a real app, you would want to secure this based on user authentication.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
